name: Hello Workflow

on: push

jobs:
  hello:
    runs-on: ubuntu-latest
    steps:
      - name: Hello
        run: echo "Hello"
      - name: Date
        run: date
      - name: Current dir
        run: ls -l
